@using SchoolTemplate.Database;
@model List<Event>
@{
    ViewData["Title"] = "Home Page";
}

<head>
    <link rel="stylesheet" href="~/css/event.css" />
</head>

    @foreach (Event e in Model)
    {
    <p class="border">
        <a class="link" href="/details/@e.id">
            @e.Plaats<br />
            @e.Datumtijd.ToString("yyyy-MM-dd HH:mm")
            @e.Prijs<br />
        </a>

    </p>

    }